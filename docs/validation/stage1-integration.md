# CookKG 第一阶段整合验收记录

日期：2026-09-10

## 整合范围

本版本基于 `main@ed3cf27`，处理两位成员的最新提交：

- `Zouyilin06/recipedatagraph@eeeab896`：v2 数据契约、100 道来源绑定标注草稿、
  本体、审核工具、NetworkX/Neo4j 数据适配器和测试；
- `98K12WQ/algorithm@daa1a1e2`：忌口与联合补购算法原型、选择组语义、API 原型和实验设计。

数据分支按原作者提交信息接入。算法分支与主线存在一套重复后端，因此采用语义移植：
保留主线 FastAPI 和 UI 契约，加入封闭食材选择组求解，并记录未移植边界。

## 当前可演示成果

| 层次 | 结果 | 使用边界 |
|---|---:|---|
| HowToCook 全量扫描 | 370 个 Markdown，369 道非模板菜谱 | 固定提交 `2b19c9e9` |
| 默认严格数据 | 10 道人工审核菜谱 | 默认 CLI、API、UI 演示使用 |
| v2 标注草稿 | 100 道，262 个食材，42 个选择组 | 人工确认数为 0 |
| v2 标准图 | 539 个节点，1,292 条关系 | 完整保留待审核语义 |
| v2 技术联调候选 | 53 道 | 只说明接口兼容，不称为已审核 |
| 联合补购 | 1—3 道组合，候选池前 60 内确定性排序 | 按食材种类，不处理克数 |
| 忌口解释 | 具体食材与类别多跳路径 | 默认严格图只使用审核关系 |
| 选择组 | 封闭食材组可求解 | 开放组、工具组继续阻断 |

v2 阻断统计为：35 道来源或标注问题、8 道缺少公开菜名、4 道工具选择组。
兼容投影发现空菜名后增加了公开字段门禁，避免 API 输出空标题和不完整解释。

## 验证结果

- Python 3.13.11：99 个测试通过，2 个在线 Neo4j 测试跳过；
- Ruff：`src`、`tests`、`scripts` 全部通过；
- v2 连续两次构建：23 个生成文件逐字节一致；
- 审核页：1,422 个内部证据链接有效，0 个预选审核框，JavaScript 语法通过；
- React：5 个测试文件、17 个组件测试通过，TypeScript 与 Vite 生产构建通过；
- Playwright：2 个模拟数据响应式流程、1 个真实 FastAPI 流程通过；
- 默认严格演示返回“空气炸锅面包片 + 洋葱炒鸡蛋”，合并补购为“葱”；
- v2 技术演示能返回菜单并沿类别图排除辣椒后代。

当前整合机器没有连接 Neo4j，因此没有重跑在线验证。Zouyilin06 的提交记录了 Neo4j
Community 5.26.30 的 539 节点、1,292 关系一致性验证；最终答辩前仍应在小组统一环境复跑。

## 下一阶段必须完成

1. 三位成员冻结标注规范，对 v2 草稿进行人工审核，先完成 20 道双人独立标注与裁决；
2. 补齐空菜名、35 道问题记录和工具选择组语义，再扩展严格推荐覆盖；
3. 为不少于 100 条评测输入填写独立人工期望结果；
4. 用同一评测集重跑关键词/图谱、贪心/联合优化、全必需/关系语义三组实验；
5. 在统一 Neo4j 环境复跑幂等导入、图属性比较和代表性查询。
