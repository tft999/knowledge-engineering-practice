# CookKG 最终阶段工作与三人验收

## 最终问题

CookKG 要同时回答两类问题：一是“在库存、忌口、菜数和采购上限下应选哪组道菜”，
二是“这些推荐、排除或做法为什么成立”。前者由知识图谱和集合运算给出确定性结论，
后者由 HybridCypher 检索图关系与菜谱原文，再由模型生成带引用回答。

## 冻结数据流

```text
HowToCook 固定提交 → 双人审核 → 标准图和 Evidence
用户菜单条件 → 归一化 → 类别展开 → 硬过滤 → 联合补购排序
用户自然语言问题 → 路由 → 文本召回 → 参数化图扩展 → 去重排序
                   → 编号证据 → LLM → 引用校验 → 页面
```

## 分工与交接

| 负责人 | 输入 | 必交产物 | 交给谁 | 验收门槛 |
| --- | --- | --- | --- | --- |
| 组长 | 数据与问答 API | 双入口 UI、集成测试、实验汇总、报告/PPT/演示 | 全组 | 新环境可启动；引用可跳转；完整排练 2 次 |
| Zouyilin06 | 100 道草稿与原文 | 双人确认记录、标准图、Evidence、Neo4j 验收 | 98K12WQ | 100 道均哈希有效；无 pending；重复导入无重复 |
| 98K12WQ | 标准图与 Evidence | 四检索器、路由、问答 API、实验脚本 | 组长 | 固定 Cypher；引用校验；失败状态明确；测试通过 |

审核合并顺序固定为数据、检索后端、UI、实验结果、答辩材料。所有模块先合入
`integration/final`，全量验证通过后进入 `main`。

## 当前实现状态（2026-09-13）

- 已实现稳定 Evidence 索引、四检索器统一接口、确定性路由、参数化 Cypher 模板；
- 已实现 OpenAI 兼容模型适配、`POST /api/v1/answers` 和引用越界/缺失检查；
- 已实现问答 UI、路由说明、证据卡片、无证据和服务失败状态；
- 已实现人工审核门禁的检索评测命令，输出 JSONL、CSV 和 Markdown；
- 当前正式人工审核仍为 10 道，v2 的 100 道为机器草稿，不能签收为 100 道人工审核；
- 当前环境尚未完成真实 Neo4j、真实模型和 100 条人工测试集的在线最终验收。

## 剩余人工任务

1. 两位标注者独立复核相同 20 道，组长裁决分歧并冻结规范。
2. 分配余下 80 道，提交审核人、日期、哈希和证据行号。
3. 三人确认 100 条问题，冻结 20 条开发集和 80 条测试集。
4. 配置 Neo4j 与模型，运行在线验收和四组实验。
5. 用真实结果替换报告中的待测项，完成两次计时排练。
